<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="com.will.studio.bestroute"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".frontend.main.Activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".frontend.settings.SettingsActivity"
            android:label="@string/title_activity_settings"/>
        <activity
            android:name=".frontend.main.Activities.NewItemActivity"
            android:label="@string/title_activity_new_item"
            android:parentActivityName=".frontend.main.Activities.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".frontend.main.Activities.MainActivity"/>
        </activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version"/>
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAGzeWRSZ52WwnQ4oqhYqHBP6dmJxBcjzc"/>

        <activity android:name=".frontend.main.Activities.MapViewActivity"></activity>

        <receiver
            android:name=".frontend.main.Receivers.BootReceiver"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"></action>
            </intent-filter>
        </receiver>
        <receiver android:name=".frontend.main.Receivers.NotificationReceiver"/>
        <receiver android:name=".frontend.main.Receivers.DismissReceiver"/>
        <receiver android:name=".frontend.main.Receivers.NaviReceiver"/>
    </application>

</manifest>
